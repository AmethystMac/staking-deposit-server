This was forked from [here](https://github.com/ethereum/staking-deposit-cli)

# Usage
- Have python 3.12 installed on your machine.
- Do `pip install -r requirements.txt` to install the necessary python packages.
- Run `server.py` on the root directory to start the server.
